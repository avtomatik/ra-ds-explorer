[project]
name = "gost-ra-client"
version = "0.1.0"
description = "Python/PySide client framework for enterprise RA and PKI REST APIs using mutual TLS authentication."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "pydantic-settings>=2.14.1",
    "uvicorn>=0.48.0",
]
